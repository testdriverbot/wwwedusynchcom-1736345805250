version: 4.1.33
steps:
  - prompt: Assert that the webpage loads without errors.
    commands:
      - command: assert
        expect: Example Domain is visible
  - prompt: >-
      Assert that the EDU SYNCH logo is visible on the page.  # Use the content
      of the file from the environment
    commands:
      - command: assert
        expect: EDU SYNCH logo is visible
